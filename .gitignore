# 1. Python 가상 환경 (필수)
# - 이 폴더는 컴퓨터마다 다르며 용량이 매우 큽니다.
venv/
.venv/

# 2. Python 캐시 파일 (필수)
# - Python이 자동으로 생성하는 임시 파일입니다.
__pycache__/
*.pyc
*.pyo
*.pyd

# 3. IDE 및 OS 설정 파일 (권장)
# - 개인 컴퓨터의 편집기 설정 파일입니다.
.vscode/
.idea/
.DS_Store
Thumbs.db

# 4. AI 모델 '뇌' (필수)
# - Git은 100MB가 넘는 대용량 파일을 관리하기에 적합하지 않습니다.
# - *.pth 파일은 절대로 Git에 올리면 안 됩니다.
*.pth
*.h5
*.onnx

# 5. 원본 데이터 및 생성된 이미지 (필수)
# - 수천 개의 이미지 파일과 원본 데이터는 Git에 올리면 안 됩니다.
원천데이터/
라벨링데이터/
analysis_output/
test_imgs/

# 6. 사용자 업로드 폴더 (필수)
# - 사용자가 업로드한 이미지는 코드가 아닌 데이터입니다.
uploads/

# 7. 데이터 압축 파일
# - 원본 데이터의 압축 파일입니다.
*.zip

# 8. 스크립트로 재생성되는 JSON 파일
# - 이 파일들은 .py 스크립트를 실행하면 다시 만들 수 있습니다.
clustered_labels.json
face_features.json
final_training_data.json